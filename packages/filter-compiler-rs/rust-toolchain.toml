[toolchain]
# Pin to a specific stable release. Bump deliberately, not automatically.
# Contributors who clone the repo get exactly this toolchain installed by rustup.
channel = "1.86.0"
components = ["rustfmt", "clippy"]
targets = ["wasm32-unknown-unknown"]
profile = "minimal"
